import 'package:flutter/material.dart';

class NavFavourite extends StatelessWidget {
  const NavFavourite({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

